# ~/.tmuxinator/aliproxyclient.yml

name: AliProxyClient
root: ~/code/tz/AliProxyClient

on_project_start: rm -f log/development.log && rm -f log/test.log

windows:
  - main:
      layout: main-horizontal
      panes:
        - vim
        -
        - tig
