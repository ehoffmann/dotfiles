<%
suffix = @args[0]

project =
  if suffix.nil? || suffix.empty? || suffix == "0"
    "kineton"
  else
    "kineton_#{suffix}"
  end

root_dir = File.expand_path("~/code/#{project}")
%>

name: <%= project %>
root: <%= root_dir %>

windows:
  - code:
      layout: main-vertical
      panes:
        - vim
        - git status
