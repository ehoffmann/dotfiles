name: leetcode
root: ~/code/leetcode

windows:
  - main:
      layout: main-vertical
      panes:
        - vim
        - ls *.c | entr -cpr -s 'gcc -Wall -Wextra -Wpedantic -fsanitize=address,undefined "$0" -lm  && time ./a.out'
        - clear;ls
