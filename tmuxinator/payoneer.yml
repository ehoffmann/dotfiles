# ~/.tmuxinator/payoneer.yml

name: Payoneer Ruby
root: ~/code/tz/payoneer-ruby

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -vvv
windows:
  - main:
      layout: main-horizontal
      panes:
        - vim
        -
        -
  - back:
      layout: even-vertical
      panes:
        -
        -
