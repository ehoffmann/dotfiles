name: server
root: /tmp

windows:
  - ssh:
      layout: main-vertical
      panes:
        - bash:
          - ssh kim
          - clear
          - ls
        - bash:
          - ssh kim
          - clear
          - ls
