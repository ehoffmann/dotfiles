name: Shopify App
root: ~/code/shopify_app

on_project_start:

windows:
  - main:
      layout: main-horizontal
      panes:
        - vim
        - git --no-pager log --pretty=reference -12
        -
  - guard:
      layout: even-vertical
      panes:
        -
        -
