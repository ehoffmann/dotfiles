# ~/.tmuxinator/t4b.yml

name: t4b
root: ~/code/tz/t4b

on_project_start: rm -f log/development.log && rm -f log/test.log && ctags -R
windows:
  - main:
      layout: main-horizontal
      panes:
        - sleep 1 && vim
        -
        -
  - guard:
      layout: even-vertical
      panes:
        - yes | dco rm
        -
