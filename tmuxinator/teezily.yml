# ~/.tmuxinator/teezily.yml
name: teezily
root: ~/teezily/teezily
pre: rm -f log/development.log && rm -f log/test.log
windows:
  - main:
      layout: main-horizontal
      panes:
        - vim
        -
        -
  - guard:
      layout: even-vertical
      panes:
        -
        -
