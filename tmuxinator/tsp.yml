# ~/.tmuxinator/tshirt-previewer.yml
name: Tshirt Previewer
root: ~/code/tz/tshirt-previewer
on_project_first_start: rm -f log/development.log && rm -f log/test.log
windows:
  - main:
      layout: main-horizontal
      panes:
        - sleep 1 && vim
        -
        -
  - guard:
      layout: even-vertical
      panes:
        -
        -
