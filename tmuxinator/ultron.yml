name: ultron
root: /tmp

on_project_exit:
  - tmux set-option -t server status-style "bg=red,fg=white,bold"

windows:
  - ssh:
      layout: main-vertical
      panes:
        - bash:
          - ssh ultron
          - clear
          - ls
        - bash:
          - ssh ultron
          - clear
          - ls
